pipeline{
    agent any
     parameters {
        string(name: 'ENV', defaultValue: 'Prod', description: 'This is to test...')
    }
    stages{
        stage("Build stage"){
            steps{
                sh 'echo "Hello World"'
            }
        }
    }
}